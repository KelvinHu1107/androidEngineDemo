<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

     <RelativeLayout style="@style/layout_title" >
            <Button style="@style/btn_back" android:id="@+id/btnBack" />
            <TextView style="@style/txt_title" android:id="@+id/txtPNSTitle" android:text="Search Kind List" />
            <Button style="@style/btn_home" android:id="@+id/btnMap" />
        </RelativeLayout>

    <RelativeLayout
        android:id="@+id/relativeLayout1"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" >

        <ListView
            android:id="@+id/listView1"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#FFFFFF"
            android:cacheColorHint="#00000000"
            android:divider="#d4d4d4"
            android:dividerHeight="1px" >

        </ListView>
    </RelativeLayout>

</LinearLayout>